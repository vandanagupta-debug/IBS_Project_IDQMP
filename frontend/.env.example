# Copy to .env.local for `npm run dev`, or pass as a Docker build-arg
# (see frontend/Dockerfile and the root docker-compose.yml) for production
# builds, since Vite bakes VITE_* vars in at build time.
VITE_API_BASE_URL=http://localhost:8000
